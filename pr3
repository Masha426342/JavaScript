let numbers = [1, 2, 3, 4, 5];// создаём список
let squareNumbers = numbers.map(num => num * num); //создаем новый список из старых чисел, возведенных в квадрат

console.log('Массив после возведения в квадрат:', squareNumbers);// выводим результат